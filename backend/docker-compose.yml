services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: workout_ai
      POSTGRES_PASSWORD: workout_ai
      POSTGRES_DB: workout_ai
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
